version: '3.8'

services:
  todo-assistant:
    build: .
    container_name: todo-assistant
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
